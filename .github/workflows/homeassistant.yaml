name: homeassistant

on:
  push:
    branches:
      - master
  pull_request:
    branches: ["master"]
  schedule:
    - cron: "0 0 * * *"

jobs:
  get-release:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.release.outputs.tag }}
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
      - name: Grab latest release tag
        id: release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          deno run --allow-net --allow-env homeassistant/get-release.ts > release.txt
          echo "tag=$(cat release.txt)" >> $GITHUB_OUTPUT

  latest:
    runs-on: ubuntu-latest
    needs: [get-release]

    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: echo stuff
        run: |
          echo ${{ needs.get-release.outputs.tag }}

      # - name: Build and push
      #   uses: docker/build-push-action@v6
      #   with:
      #     file: ./homeassistant/Dockerfile
      #     push: true
      #     tags: ${{ secrets.DOCKERHUB_USERNAME }}/homeassistant-hacs:${{ needs.get-release.outputs.tag }}

  # repository:
    # runs-on: ubuntu-latest
    # needs: [latest]

    # steps:
      # - uses: actions/checkout@v4

      # - name: Docker Hub Description
      #   uses: peter-evans/dockerhub-description@v3
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}
      #     repository: ${{ secrets.DOCKERHUB_USERNAME }}/homeassistant-hacs
      #     readme-filepath: ./homeassistant-hacs/README.md
      #     short-description: "Homeassistant and Hacs bundled together"
